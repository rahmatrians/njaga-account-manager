// const images = import('../assets/category-icons', true);
// const images = import.meta.glob("../assets/category-icons/*")


// export const iconBank = () => {
//     const imageList = images.keys().map(image => images(image));
//     return imageList;
// }

// export const iconBank = (fileName) {
//     let ext = '.png' // can be anything
//     const imgUrl = new URL(`../assets/category-icons/${fileName}.${ext}`, import.meta.url).href
//     return imgUrl;
// }
